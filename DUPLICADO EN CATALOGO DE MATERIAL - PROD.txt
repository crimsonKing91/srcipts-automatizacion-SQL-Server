DUPLICADO EN CATALOGO DE MATERIAL - PRODUCTO
-- CHECAR SI ES MAT O AF, REGULAR ERROR

SELECT CVE_PRODUCTO
FROM PRODUCTOS
GROUP BY CVE_PRODUCTO
HAVING COUNT(*) > 1


SELECT TOP 2 * FROM MATERIAL

SELECT CLAVE
FROM MATERIAL
GROUP BY CLAVE
HAVING COUNT(*) > 1

SELECT * FROM MATERIAL 
WHERE CLAVE IN (
    SELECT CLAVE 
    FROM MATERIAL 
    GROUP BY CLAVE 
    HAVING COUNT(*) > 1
)
ORDER BY CLAVE;
